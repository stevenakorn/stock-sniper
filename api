export default async function handler(req, res) {
  const token = process.env.FINMIND_TOKEN; 
  
  // 如果這行印出來是 undefined，代表變數沒設定成功
  if (!token) {
    return res.status(400).json({ 
        error: "Token 未抓取到，請檢查 Vercel 環境變數", 
        debug_token_is_undefined: true 
    });
  }

  // ... 後續程式碼 ...
}
